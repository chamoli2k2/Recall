import { useEffect, useState } from 'react';
import { ImagePlus, X, Plus, Save, ArrowRight, RotateCcw, History } from 'lucide-react';
import { toast } from 'sonner';
import { Modal, Field, Button, ErrorState, Tag } from './ui';
import { useApp } from '../hooks/useApp';
import { api, imageUrl } from '../services/api';
export default function CardEditor({ folder, card, onClose }) {
  const { refresh, user } = useApp(); const blank = { front: { text: '', image: null }, back: { text: '', image: null }, tags: [], hint: '', source: '' };
  const key = `recall-draft:${user.id}:${folder.id}:${card?.id || 'new'}`;
  const [form, setForm] = useState(() => { try { const draft = JSON.parse(sessionStorage.getItem(key)); if (draft) return draft; } catch {} return card ? { front: card.front, back: card.back, tags: card.tags, hint: card.hint, source: card.source } : blank; });
  const [tag, setTag] = useState(''), [busy, setBusy] = useState(false), [uploading, setUploading] = useState(''), [error, setError] = useState(''), [history, setHistory] = useState(null);
  useEffect(() => { try { sessionStorage.setItem(key, JSON.stringify(form)); } catch {} }, [form, key]);
  const update = (field, value) => setForm(f => ({ ...f, [field]: value }));
  const updateSide = (side, patch) => setForm(f => ({ ...f, [side]: { ...f[side], ...patch } }));
  function addTag() { const next = tag.trim().toLowerCase().replace(/^#/, '').slice(0, 30); if (next && !form.tags.includes(next) && form.tags.length < 10) update('tags', [...form.tags, next]); setTag(''); }
  async function upload(side, file) { if (!file) return; if (file.size > 5 * 1024 * 1024) { setError('Choose an image smaller than 5 MB.'); return; } setUploading(side); setError(''); try { const data = new FormData(); data.append('image', file); const r = await api(`/folders/${folder.id}/images`, { method: 'POST', body: data }); updateSide(side, { image: r.id }); } catch (e) { setError(e.message); } finally { setUploading(''); } }
  async function save(another = false) { for (const side of ['front', 'back']) if (!form[side].text.trim() && !form[side].image) { setError('Add text or an image to both sides.'); return; } setBusy(true); setError(''); try { const finalTags = tag.trim() ? [...new Set([...form.tags, tag.trim().toLowerCase().replace(/^#/, '')])].slice(0, 10) : form.tags; await api(card ? `/cards/${card.id}` : `/folders/${folder.id}/cards`, { method: card ? 'PATCH' : 'POST', body: { ...form, tags: finalTags, ...(card ? { version: card.version } : {}) } }); sessionStorage.removeItem(key); refresh(); toast.success(card ? 'Card updated' : 'Flashcard created'); if (another) { setForm(blank); setTag(''); } else onClose(); } catch (e) { setError(e.message); } finally { setBusy(false); } }
  return <Modal open onClose={onClose} wide title={card ? 'Make it memorable' : 'One new thing to remember'} description={`${folder.title} · Drafts are kept in this browser tab until saved.`}><form className="form-stack" onSubmit={e => { e.preventDefault(); save(); }}><div className="card-editor-sides">{['front', 'back'].map((side, i) => <div className="side-editor" key={side}><div className="side-label"><span>{i ? '02' : '01'}</span>{i ? 'BACK · ANSWER' : 'FRONT · QUESTION'}</div><textarea aria-label={`${side} text`} autoFocus={!i} maxLength={10000} placeholder={i ? 'The answer, in your own words…' : 'What do you want to remember?'} value={form[side].text} onChange={e => updateSide(side, { text: e.target.value })} onPaste={e => { const file = [...e.clipboardData.items].find(i => i.type.startsWith('image/'))?.getAsFile(); if (file) { e.preventDefault(); upload(side, file); } }}/>{form[side].image && <div className="editor-image"><img src={imageUrl(form[side].image)} alt={`${side} attachment`}/><button type="button" className="icon-button" aria-label={`Remove ${side} image`} onClick={() => updateSide(side, { image: null })}><X size={17}/></button></div>}<div className="editor-footer"><label className="upload-button"><ImagePlus size={17}/>{uploading === side ? 'Uploading…' : 'Add image'}<input type="file" accept="image/png,image/jpeg,image/webp" disabled={!!uploading} onChange={e => upload(side, e.target.files[0])}/></label><small>or paste a screenshot</small></div></div>)}</div><Field label="Tags · optional"><div className="tag-input">{form.tags.map(t => <span className="tag" key={t}>{t}<button type="button" aria-label={`Remove ${t} tag`} onClick={() => update('tags', form.tags.filter(v => v !== t))}><X size={12}/></button></span>)}<input aria-label="New tag" placeholder="Type a tag, press Enter" value={tag} maxLength={30} onChange={e => setTag(e.target.value)} onBlur={addTag} onKeyDown={e => { if (e.key === 'Enter' || e.key === ',') { e.preventDefault(); addTag(); } }}/></div></Field><div className="two-columns"><Field label="Hint · optional"><input placeholder="A little nudge in the right direction" value={form.hint} maxLength={1000} onChange={e => update('hint', e.target.value)}/></Field><Field label="Source link · optional"><input type="url" placeholder="https://…" value={form.source} onChange={e => update('source', e.target.value)}/></Field></div>{card && <button type="button" className="text-button history-trigger" onClick={async () => { try { const r = await api(`/cards/${card.id}/revisions`); setHistory(r.revisions); } catch (e) { setError(e.message); } }}><History size={16}/> View version history</button>}{history && <div className="revision-list">{!history.length ? <p>No previous versions yet.</p> : history.map(r => <div key={r.id}><div><strong>Version {r.version + 1}</strong><span>{r.snapshot.front?.text || 'Image card'} · {r.editor?.name}</span></div><button type="button" className="text-button" onClick={() => { setForm({ ...r.snapshot, front: r.snapshot.front, back: r.snapshot.back }); setHistory(null); toast.info('Previous version loaded into your draft. Save to restore it.'); }}><RotateCcw size={15}/> Use version</button></div>)}</div>}{error && <ErrorState message={error}/>}<div className="modal-actions"><Button type="button" className="secondary" onClick={onClose}>Close</Button><div>{!card && <Button type="button" className="secondary" disabled={busy || !!uploading} onClick={() => save(true)}>Save & add another</Button>}<Button type="submit" className="primary" loading={busy} disabled={!!uploading}><Save size={16}/>{card ? 'Save changes' : 'Create flashcard'}</Button></div></div></form></Modal>;
}
